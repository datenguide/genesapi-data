na_values: ['x', '-', '.']
encoding: latin1
delimiter: ';'
dtype: str
decimal_separator: ','
# filter:
#   _id: 'lambda x: len(x) == 5'
post_maps:
  _date:
    target: _year
    func: 'lambda x: "year:%s" % x[-4:] if x else x'
non_num_names:
  - _id
  - _name
to_num: true
